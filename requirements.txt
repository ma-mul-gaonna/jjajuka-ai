# --- Core AI & LLM ---
langchain>=0.1.0
langchain-google-genai>=1.0.0   # Gemini 2.5 Flash 연동용
langgraph>=0.0.10               # 워크플로우 제어(에이전틱 워크플로우)

# --- Mathematical Optimization ---
ortools>=9.8.0                  # Google OR-Tools (스케줄링 계산 엔진)

# --- Environment & Data ---
python-dotenv>=1.0.0            # .env 파일의 API_KEY 로드용
pydantic>=2.0.0                 # 데이터 검증 및 스키마 정의

# --- API & Server (FastAPI 선택 시 필요) ---
# fastapi>=0.100.0
# uvicorn>=0.20.0

# --- Utilities ---
numpy>=1.24.0                   # 수치 계산 보조 (필요 시)
pandas>=2.0.0                   # 데이터 프레임 처리 (리포트용)